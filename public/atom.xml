<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://inspired-minds.vercel.app</id>
    <title>My Inspired-Minds</title>
    <updated>2026-05-13T02:16:51.179Z</updated>
    <generator>Feed for Node.js</generator>
    <author>
        <name>FisherCat</name>
        <email>fishercat_@outlook.com</email>
        <uri>https://inspired-minds.vercel.app</uri>
    </author>
    <link rel="alternate" href="https://inspired-minds.vercel.app"/>
    <link rel="self" href="https://inspired-minds.vercel.app/atom.xml"/>
    <subtitle>Record inspired moments and thoughts</subtitle>
    <logo>https://inspired-minds.vercel.app/fishercat.png</logo>
    <icon>https://inspired-minds.vercel.app/fishercat.png</icon>
    <rights>All rights reserved 2026, FisherCat</rights>
    <entry>
        <title type="html"><![CDATA[Rethinking Concept Bottleneck Model: From Pitfalls to Solutions]]></title>
        <id>https://inspired-minds.vercel.app/blog/CBM_Suite</id>
        <link href="https://inspired-minds.vercel.app/blog/CBM_Suite"/>
        <updated>2026-05-12T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[CVPR'26 | Rethinking the effectiveness of CBMs, is it makes interpretable decisions? and How?]]></summary>
        <content type="html"><![CDATA[<blockquote>
<p><a href="https://arxiv.org/abs/2603.05629">Paper</a></p>
</blockquote>
<p><img src="/CBM_Suite/arch.png" alt="arch"></p>
<h2>Motivation</h2>
<p><img src="/CBM_Suite/mov.png" alt="mov"></p>
<p>虽然现有的 CBM 方法从人类可理解的层面解释模型的决策，但仍存在一些局限。现有方法往往关注模型的可解释性和性能方面，没有关注到更深层次的重要问题。本文首先对现有 CBM 方法在解释性和模型结构上的问题进行了阐述，并提出了相应的应对措施。</p>
<h2>Concept Set Relevance</h2>
<p>语言引导的概念瓶颈模型（LG-CBM，如LF-CBM、LaBo、LM4CV、VLGCBM等）通过提问大语言模型来获取与目标类别相关的概念描述，但这些概念中存在对模型分类没有帮助的概念，例如背景、环境、习性等。现有方法通过设计不同的筛选方式来进一步选择视觉和语义层面一致的概念描述，作为模型的概念池（concept pool）。本文作者提出通过计算概念与样本之间的熵，作为衡量概念集与数据集中样本是否匹配的依据。具体来说，给定一个样本$x_i$, $c_i \in \mathbb{R}^K$是该样本对应的概念激活分数，计算公式为：
$$
c_i = \frac{f_I(x_i)\cdot f_T(\mathbb{C}) - \mu}{\sigma}
$$</p>
<p>其中，$f_I$和$f_T$分别表示图像编码器和文本编码器，$\mathbb{C}$ 表示概念池中的概念集合。$\mu$和$\sigma$表示概念激活分数的均值和标准差。由此可以得出概念激活分数在下游任务数据集上的分布，作者对比了无关概念集与相关概念集分布的差异：</p>
<p><img src="/CBM_Suite/distribution_diff.png" alt="distribution_diff"></p>
<p>从上图中可以看到，无关概念集在目标数据集上的概念分数分布接近于正态分布，而相关概念集的概念分数分布更加集中，呈现“更高、更窄”的特性。作者认为，好的概念解释对应的概念分数应当是稀疏的，激活概念与未激活概念的差值大。基于此，作者提出利用概念激活分数的熵来评估概念集与当前任务的相关性：
$$
c_{i,k}&#39; = \frac{\exp (c_{i,k})}{\sum_{j=1}^K \exp (c_i,j)}
$$</p>
<p>$$
H(c_{i,k}&#39;) = -\sum_{j=1}^K c_{i,j}&#39; \log c_{i,j}&#39;
$$</p>
<p>首先对所有概念激活分数求 softmax，然后计算它们的熵$H$。</p>
<ul>
<li>低熵：少数概念占据主要概率，说明概念激活集中，解释更稀疏、更可能有意义。</li>
<li>高熵：很多概念激活差不多，说明概念集对图像没有清晰响应，更像噪声。</li>
</ul>
<p>完成熵值计算后，本文从两个层面评估概念集的相关性。</p>
<ol>
<li>Task-agnostic：评估概念集合对整体数据集的相关性，计算所有样本概念熵值的平均。</li>
<li>Task-specific：评估概念集合对每个类别的相关性，计算所有类别概念熵值的平均。</li>
</ol>
<p>在主流的 CBM 结构中，目标样本由图像编码器（CNN、VIT等）提取其图像特征，然后通过概念瓶颈层（CBL）将图像特征映射到概念分数空间，得到图像对所有概念的激活分数，最后由分类器层完成分类。这种情况下，CBM 中所包含的概念是人为确定的，CBL 中的神经元被人为指定代表某个概念。从网络结构上来看，其实就是在Backbone 和 Classifier 之间加入一层 Linear 或 MLP，构建可解释的中间表示。因此，如果没有较强的可解释性约束，CBL 层输出的中间表示可能不足以提供解释性，而概念集是否与目标数据集相关也不会影响到模型的性能。</p>
<h2>Linearity Problem</h2>
<p>现有的 LG-CBM 方法往往直接利用 Linear 或 MLP 构建 CBL，将图像特征映射到概念分数空间。作者认为这样的设计绕过了概念瓶颈的结构，即使有多个线性映射，它们最终可以融合成一个单一的线性变换，模型整体退化为线性变换的分类模型，导致模型在实际分类的过程中没有真正利用到概念瓶颈。</p>
<p>而早期的 CBM 方法中有提到使用非线性激活层来构建 CBL。例如原始<a href="https://arxiv.org/abs/2007.04612">CBM</a>方法中提出在构建概念分数到类别空间的映射时，插入非线性激活层 ReLU 来构建 CBL：</p>
<pre><code class="language-text"># 原始CBM方法在OAI数据集上的工作流程
image -&gt; ResNet-18 -&gt; Linear -&gt; concepts
concepts -&gt; MLP(50 hidden units, 50 hidden units) -&gt; KLG grade
</code></pre>
<p>本文作者重申该观点，认为 CBM 应当避免完全构建为线性变换的结构，并通过实验验证了纯线性变换结构会对损害模型的可解释性。</p>
<p>但仔细来看，二者又有不同之处。CBM 提出利用 ReLU 来构造 CBL，是出于概念组合和层级的假设，让 CBL 层能够表达复杂的概念关系。例如在 OAI 数据集上的分级任务，标签 $y$ 往往不是概念的简单线性加权，而可能依赖：</p>
<ul>
<li>概念组合：两个症状一起出现才强烈指向某个等级；</li>
<li>阈值效应：某个概念超过一定严重程度后，标签变化很大；</li>
<li>饱和效应：概念继续变严重，但标签等级不再线性增加；</li>
<li>非对称关系：轻微骨刺和严重关节间隙变窄的影响不同。</li>
</ul>
<p>在原始CBM中，线性分类器只能学到概念之间线性的关系对分类的影响，而利用非线性激活层可以学习如上复杂的概念关系以及如何据此进行分类。此外，原始CBM 方法中在 OAI 数据集上的实验表明了带有非线性层的CBL 设计可能具有更好的干预性能。而在另一个数据集 CUB 上，原始 CBM 方法还是应用了 Linear 作为 CBL 层，这也说明构建 CBM 时非线性层并不是必要的，这一点在本文当中没有说明。</p>
<p>而在本文中，作者认为纯线性变换结构会影响模型的可解释性，导致模型对概念集不敏感，即使是随机的概念描述模型也能够做出正确的预测：</p>
<p><img src="/CBM_Suite/linearity_problem.png" alt="linearity problem"></p>
<p>这种情况在 LG-CBM 方法中比较明显。由于缺乏显式概念标签，模型直接将提取到的概念的文本特征堆叠作为概念瓶颈（LaBo，LM4CV），或通过 CLIP 软标签的形式来约束 CBL 输出与 CLIP 一致，保证模型能够提供合理的解释。但在随机概念的情况下，CLIP 得到的概念激活矩阵也是无意义的，CBM 依据随机的概念进行分类。在交叉熵损失的引导下，模型并不知晓概念是否与数据集中的样本相关联，所以需要额外的方式去判断。</p>
<p>此外，本文作者进行实验发现，具有非线性层的CBM对概念集更加敏感：</p>
<p><img src="/CBM_Suite/acc_comp.png" alt="acc_comp_linear_non_linear"></p>
<p>我们从图中可以看到，Linear CBM 的性能受概念集变化的波动比较小，Non-Linear CBM 受概念集变化的波动较大。作者认为Non-Linear CBM 会依赖于有意义的概念表示来进行预测，Linear CBM 可以通过简单的线性变换完成分类。</p>
<p>但这里的核心区别在于是否使用非线性层，而激活非线性层的神经元需要输入达到一定阈值，这是它的特性（如ReLU）。本文方法通过计算CBL输出与监督 VLM 模型 SAIL 输出的激活分数之间的MSE损失引导模型的概念学习过程，模型是否学习到正确的类别-概念关系关键在于 SAIL 的预训练知识中是否含有概念类别之间的正确联系。因此可以进一步猜想性能下降的本质原因：</p>
<p>对于 relevant concepts，文本概念与图像类别之间更可能存在语义对应关系，因此图像-概念相似度或概念预测分数更容易形成有效的、可区分的激活模式。非线性层，如 ReLU，在这种情况下更多保留任务相关的正向信号，使后续分类器仍能从概念表示中获得有用信息。</p>
<p>而对于 irrelevant concepts，尤其是 random words，这些概念很难在语义上描述目标样本。图像与随机词之间的相似度往往缺少稳定正向语义关联，概念分数可能更低、更分散，或者更接近噪声。经过 ReLU 等非线性激活后，较多神经元会被截断为零，导致 concept bottleneck 中可传递的信息减少。于是，模型后续分类器接收到的表示不仅语义无关，而且信息量也被进一步压缩，分类性能下降更明显。从上图中我们也可以看到，从 Linear CBM 到 Non-Linear CBM，模型在 relevant / irrelevant 概念集上性能的变化，可以明显观察到在 relevant 概念集上，引入非线性层带来的性能下降要比在 irrelevant 概念集的幅度低很多，一定程度上可以佐证我们的猜测。</p>
<h2>Method</h2>
<p><img src="/CBM_Suite/arch.png" alt="arch"></p>
<h3>Concept Alignment</h3>
<p>本文所提出方法 CBM-Suite的模型结构如上图所示，在 CBL 中插入了非线性层。本文采用与 LF-CBM 相同的范式，通过 VLM 的激活分数矩阵引导模型的概念学习过程。具体来说，本文计算CBL层输出的概念分数与VLM的激活分数之间的 MSE 损失：</p>
<p>$$
L_{CE} = \frac{1}{N}\sum_{i=1}^N ||\hat{c}_i - c_i||^2
$$</p>
<h3>Teacher-Guided Training</h3>
<p>为了弥补非线性层加入带来的模型性能损失，本文额外设置一个教师，直接基于图像特征完成分类，通过蒸馏教师和 CBM 输出的 logits 来提升模型的分类性能。利用交叉熵损失指导训练过程：</p>
<p>$$
L_T = -\sum_{i=1}^N y_i\log(\text{softmax}(W_t\cdot f(x_i)))
$$</p>
<p>其中 $W_t$ 表示教师分类器，$f(x_i)$ 表示图像特征。训练好教师后，结合蒸馏损失、交叉熵损失和稀疏损失指导分类器的训练过程：</p>
<p>$$
L_C = \alpha \cdot [-\sum_{i=1}^N y_i\log(\text{softmax}(W_f \cdot \hat{c_i})) + \lambda ||W_f||<em>1 +(1-\lambda) ||W_f||^2 ] \ +  \beta \cdot T^2 \sum</em>{i=1}^N z_i(\log (\frac{W_f \hat{c_i}}{T} - \log \frac{z_i}{T}))
$$</p>
<h2>Experiments</h2>
<p>CBM-Suite 与 其他 SOTA 方法的性能对比：</p>
<p><img src="/CBM_Suite/maintab.png" alt="maintab"></p>
<p>应用不同的 backbone 和 VLM 对模型性能的影响（IN100）：</p>
<p><img src="/CBM_Suite/backbone_vlm_pair.png" alt="backbone_vlm_pair"></p>
]]></content>
        <author>
            <name>FisherCat</name>
            <email>fishercat_@outlook.com</email>
            <uri>https://inspired-minds.vercel.app</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[Partially Shared CBM]]></title>
        <id>https://inspired-minds.vercel.app/blog/PSCBM</id>
        <link href="https://inspired-minds.vercel.app/blog/PSCBM"/>
        <updated>2026-05-11T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[AAAI'26 | Concepts are shared & labled by different classes, improvement on semantic level of CBL.]]></summary>
        <content type="html"><![CDATA[<blockquote>
<p><a href="https://arxiv.org/abs/2511.22170">Paper</a></p>
</blockquote>
<p><img src="/PSCBM/featured.png" alt="Arch"></p>
<h2>Motivation</h2>
<p>今年来的CBM方法（LaBo, LF-CBM）通过提问大语言模型（ChatGPT）的方式来构建概念库，但还是存在一些问题：</p>
<ul>
<li>概念冗余</li>
<li>概念与视觉样本关联性低</li>
<li>概念不够紧凑（compactness）
<img src="/PSCBM/motivation.png" alt="mov"></li>
</ul>
<p>作者认为LaBo、LF-CBM等方法为每个类别生成概念描述，使得概念库有很多冗余；而让所有类别共享全局概念又会影响模型的预测。故本文提出：</p>
<ol>
<li>多模态概念生成，构造在视觉样本中真实存在的概念</li>
<li>通过概念融合策略减少概念冗余，在类间部分共享概念
在这里我们更加关注该方法的合并和标注策略。</li>
</ol>
<h2>Concept Merge &amp; Label</h2>
<p>本文提出的概念融合方法是基于相似度计算的
<img src="/PSCBM/alg_merge.png" alt="alt text"></p>
<p><strong>I. 概念筛选</strong>.  PSCBM首先计算样本与所有获取到的概念之间的相似度，得到Affinity矩阵：</p>
<p>$$
A_{i,j} = \cos(\Phi(x_i), \Psi(c_j))
$$</p>
<p>其中，每一行代表的是样本与所有概念之间的相似性分数。随后计算概念之间的相关性矩阵$Q$：</p>
<p>$$
Q_{i,j} = \frac{A_{:, i}^\top A_{:,j}}{||A_{:,i}|| ||A_{:,j}||}
$$</p>
<p>该矩阵本质上计算的是Affinity矩阵中概念与概念之间的相似性，$A_{:,i}$代表的就是第$i$个概念对所有数据样本的相似度向量。</p>
<p><strong>II. 概念融合</strong>.  在计算得到相关性矩阵$Q$后，选择其中分数大于阈值$\tau_{merge}$的概念作为待融合的概念。根据算法流程图，下一步将选择概念组进行融合。一个概念组$S_j = {c_2, c_4,..., c_n}$由多个概念构成，在语义层面上说明概念组$S_j$中的这些概念与$c_j$相似度高，需要进行融合。随后从所有的概念组中选择包含概念数量最多的概念组先进行融合：</p>
<p>$$
c_{max} \leftarrow \argmax_{c_j \in S} |S_j|
$$</p>
<p>$$
\hat{S} \leftarrow \hat{S} \cup c_{max}
$$</p>
<p>$$
S \leftarrow \ (c_max \cup S_{max})
$$</p>
<p>其中，$c_{max}$表示所含概念最多的概念组对应的概念，$S_{max}$表示$c_{max}$对应的概念组。完成概念筛选和融合后，我们就得到了最终的概念库。</p>
<p><strong>III. 概念标注</strong>.  现在我们得到的概念库还没有标签，也就是说图像数据和概念之间的联系不确定。本文提出基于Affinity矩阵来进行概念标注：</p>
<p>$$
s_{i, j}=\left{\begin{array}{ll}
1, &amp; \text { if } y_{i} \in C_{j} \text { and } \boldsymbol{A}<em>{i, j}&gt;\tau</em>{\text {conf }} \
0, &amp; \text { otherwise }
\end{array}\right.
$$</p>
<p>其中，$s_{i, j}$表示样本$i$对概念$j$的标签，$C_{j}$表示类别$j$相关的概念集合。</p>
]]></content>
        <author>
            <name>FisherCat</name>
            <email>fishercat_@outlook.com</email>
            <uri>https://inspired-minds.vercel.app</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[Convolutional Neural Network]]></title>
        <id>https://inspired-minds.vercel.app/blog/CNN</id>
        <link href="https://inspired-minds.vercel.app/blog/CNN"/>
        <updated>2025-07-29T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[The principle of CNN]]></summary>
        <content type="html"><![CDATA[<h2>Intro</h2>
<p>卷积神经网络（Convolutional Neural Network，CNN）是一种在计算机视觉领域取得了巨大成功的深度学习模型。它们的设计灵感来自于生物学中的视觉系统，旨在模拟人类视觉处理的方式。在过去的几年中，CNN已经在图像识别、目标检测、图像生成和许多其他领域取得了显著的进展，成为了计算机视觉和深度学习研究的重要组成部分。</p>
<p>我们希望模型能够不受图像中的物体在图中的位置、大小等几何变换的影响，正确的做出判断，这一特点就是不变性。CNN在卷积层中的卷积操作可以捕捉物体在图像中的局部特征而不受位置等变换的影响。</p>
<h2>为什么使用卷积神经网络</h2>
<p>使用全连接网络处理图像时，有非常明显的缺点：</p>
<ul>
<li>将图像展开为向量会丢失空间信息</li>
<li>参数过多处理效率低下，训练困难</li>
<li>大量的参数也会容易使得网络快速过拟合</li>
</ul>
<p>而CNN通过卷积层可以很好的解决这些问题</p>
<h2>卷积操作</h2>
<h3>卷积操作是什么？</h3>
<p>卷积（Convolution）从数学层面上来看十分抽象，但通过图像可以很直观的理解：
<img src="/CNN/simple_conv.webp" alt="simple_conv"><br>从上图可以看出，卷积操作通过一个滑动窗口与输入图像（下方蓝色矩阵）进行某种操作，提取出图像的卷积后的特征（上方绿色矩阵）。 卷积操作其实就是每次取一个特定大小的矩阵$K$（蓝色矩阵中的阴影部分），然后将其对输入图像$X$（图中蓝色矩阵）依次扫描并进行内积的运算过程，最后得到卷积后的特征$F$。  </p>
<p>我们将K称之为卷积核（Convolutional Kernal / filter），可以是一个也可以是多个，卷积后得到的结果$F$称为特征图（Feature Map）。</p>
<h3>卷积操作的计算</h3>
<p>下面通过直观的图像来解释卷积的计算过程，首先是单通道单卷积核，了解之后可以扩展到多通道多卷积核的情况。</p>
<h4>单通道单卷积核</h4>
<p>如下图所示，现有一<code>[5, 5, 1]</code>形状的灰度图，需要用右边的卷积核对其进行卷积处理，过程是怎么样的呢？<br><img src="/CNN/single_channel_single_kernal.jpg" alt="single_channel_single_kernal"><br>根据上一节介绍，卷积操作要用卷积核对输入图像进行滑动计算的过程，那么可以得到：
<img src="/CNN/single_channel_single_kernal_cal.jpg" alt="single_channel_single_kernal_cal"><br>所以卷积图中的每一个元素，都是通过卷积核与输入图像对应位置的内积+bias得到的，通过卷积也将输入图像的维度进行了缩减，得到形状为<code>[3, 3, 1]</code>的特征图。</p>
<h4>单通道多卷积核</h4>
<p>单通道多卷积核与单通道单卷积核相比卷积核更多，所以最后得到的特征图也更多：
<img src="/CNN/single_channel_multi_kernal.jpg" alt="single_channel_multi_kernal">
<img src="/CNN/single_channel_multi_kernal_cal.jpg" alt="single_channel_multi_kernal_cal">   </p>
<h4>单通道多卷积核</h4>
<p>在实际生活中，我们遇到彩色的图像的情况更多，这样就需要卷积层能够处理多通道的数据。在这里需要注意的是，卷积层中对于一个多通道的输入图像，每一个卷积核也分多个通道去进行卷积操作。<br><img src="/CNN/multi_channel_single_kernal.jpg" alt="multi_channel_single_kernal">
<img src="/CNN/multi_channel_single_kernal_cal.jpg" alt="multi_channel_single_kernal_cal">   </p>
<h4>多通道多卷积核</h4>
<p>通过上面的介绍，很容易扩展到多通道多卷积核的情况：<br><img src="/CNN/multi_channel_multi_kernal.jpg" alt="multi_channel_multi_kernal">
<img src="/CNN/multi_channel_multi_kernal_cal.jpg" alt="multi_channel_multi_kernal_cal"><br>通过上面的计算，我们可以发现：  </p>
<ul>
<li>输入图像有多少个通道，其对应的卷积核就有多少个通道</li>
<li>用k个卷积核对输入图像进行处理，那么最后得到的特征图就一定会包含有k个通道</li>
</ul>
<h2>卷积结构类型</h2>
<h3>标准卷积</h3>
<p>标准卷积就是上述讨论过的卷积计算流程，每一个卷积核与输入的图像进行点积存操作得到特征图：<br><img src="/CNN/standard_conv.jpg" alt="standard_conv"></p>
<h3>分组卷积</h3>
<p>分组卷积是减少卷积计算量的一种方法。因为最后的特征图的数量越来越多，卷积核的通道数也越来越多，导致计算量逐步增加。分组卷积是将输入特征分组，每个卷积核只需要与其中的一组进行计算，每个卷积核的通道数只与每组特征图的通道数相同，下图是分组为2的例子：<br><img src="/CNN/group_conv.png" alt="group_conv">
<img src="/CNN/group_conv_result.png" alt="group_conv_result"></p>
<h3>1x1 卷积(Pointwise Convolution)</h3>
<p>在卷积神经网络中，卷积核通常是对三维feature map进行操作，这时，1×1卷积可以看作对某个局部的加权求和。1×1卷积最早出现在Network In Network的论文中 ，使用1×1卷积是想加深加宽网络结构，通常有两方面的作用：  </p>
<ol>
<li><strong>降维</strong>：比如一张500×500且通道数为100的图片在20个filter上做1×1的卷积，输出特征图的尺寸变为500×500×20。当filter数量为200时，输出特征图的尺寸会变为500×500×200，这时实现了网络加宽，或者说是升维。</li>
<li><strong>增加网络层的非线性</strong>：它的卷积过程其实相当于全连接层的计算过程，并且还加入了非线性的激活函数（relu等），从而使网络结构变得更加的复杂。</li>
</ol>
<h3>深度卷积</h3>
<p>深度卷积是分组卷积的延伸，深度卷积将每个输入特征图分为一组，这样对应的卷积核的通道数为1。<br><img src="/CNN/deep_conv.png" alt="deep_conv"><br>这样直接对于每个特征图进行标准的卷积操作。但存在一个问题，就是每个特征图由对应的卷积核计算，那么其中的信息只在对应卷积核得到的特征图中保存，也就是说第一个输出特征仅依赖于第一个输入特征，这样的模式会在网络的更深层次中持续。而只有一组的卷积操作中，卷积核是与所有的输入特征图进行计算，运用了全部的输入特征信息进行计算，有更好的全局表达能力。为了解决这个问题，人们提出在深度卷积后面加上一个点对点卷积（pointwise convolution）来融合特征的全局信息，这样的卷积模式也称为深度可分离卷积（depthwise-separable convolution）。<br><img src="/CNN/depthwise_separable_conv.png" alt="depthwise_separable_conv"></p>
<h3>空洞卷积(Dilated Convolution)</h3>
<p>空洞卷积也叫扩张卷积或者膨胀卷积，是针对图像语义分割问题中下采样会降低图像分辨率、丢失信息而提出的一种卷积思路。空洞卷积是在卷积计算的过程中对输入特征图的位置间隔计算，让原本的卷积核获得更大的感受野。<br><img src="/CNN/dilated_conv.webp" alt="dilated_conv"><br>间隔的大小可以通过扩张率(Dilation Rate)来进行控制。这样来看标准卷积就是扩张率为0的空洞卷积：<br><img src="/CNN/dilation_rate.jpg" alt="dilation_rate">   </p>
<h3>转置卷积(Transposed Convolutions)</h3>
<p>转置卷积又称为反卷积(Deconvolution)，它和空洞卷积的思路相反。计算方式是先对输入特征图中的元素中间补0分隔，使得输入特征图的size变大，然后在修改后的特征图上进行标准卷积。<br><img src="/CNN/transposed_conv.jpg" alt="transposed_conv"></p>
<h2>CNN其他组成结构</h2>
<h3>池化层(Pooling)</h3>
<h3>全连接层(Full connected)</h3>
<h3>残差连接(Residual Link)</h3>
<h2>感受野（Reciptive Field）</h2>
<p>感受野指的是卷积神经网络每一层输出的特征图上每个像素点映射回输入图像上的区域大小。神经元感受野的范围越大表示其能接触到的原始图像范围就越大，也意味着它能学习更为全局，语义层次更高的特征信息；相反，范围越小则表示其所包含的特征越趋向局部和细节。从结构上来看，感受野的大小与第一层卷积的卷积核大小很相关。<br>深度卷积神经网络中靠前的层感受野较小，提取到的是图像的纹理、边缘等局部的、通用的特征；靠后的层由于感受野较大，提取到的是图像更深层次、更具象的特征。因此在迁移学习中常常会将靠前的层的参数冻结（不参与训练，因为他们在迁移到新的场景之前已经具备了提取通用特征的能力），来节省训练的时间和算力消耗。</p>
<h2>References</h2>
<ol>
<li><a href="https://blog.csdn.net/AI_dataloads/article/details/133250229">CSDN - 【深度学习】一文搞懂卷积神经网络（CNN）的原理（超详细）</a></li>
<li><a href="https://zhuanlan.zhihu.com/p/561991816">知乎 - 【综述】一文读懂卷积神经网络</a></li>
<li><a href="https://space.bilibili.com/180599937/lists/1396446?type=season">bilibili - 3D卷积神经网络</a></li>
</ol>
]]></content>
        <author>
            <name>FisherCat</name>
            <email>fishercat_@outlook.com</email>
            <uri>https://inspired-minds.vercel.app</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[KV Cache]]></title>
        <id>https://inspired-minds.vercel.app/blog/KV_Cache</id>
        <link href="https://inspired-minds.vercel.app/blog/KV_Cache"/>
        <updated>2025-02-28T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[How KV Cache works for Transformers]]></summary>
        <content type="html"><![CDATA[<h2>KVCache</h2>
<p>KVCache（Key-Value缓存）是Transformer模型在自回归生成任务​（如文本生成、机器翻译）中用于加速<strong>推理</strong>的一种优化技术。它通过缓存历史时间步的Key和Value向量，避免重复计算，将自注意力（Self-Attention）的计算复杂度从O(n²)降低到O(n)，显著提升长序列生成的效率。(from DP-R1)</p>
<p>其核心思想还是<strong>以空间换时间</strong>，通过缓存K、V，可以避免Linear层的重复计算，从而降低计算量。在transformer的推理过程中， encoder部分接收用户输入的prompt进行编码，得到其高维特征；decoder部分会持续输出模型预测的token，每个token表示一个中文字或英文单词。此外，当模型输出完一个token后，会将其与之前输出的token拼接在一起，然后再次送入到decoder中计算。</p>
<p>KVCache 技术在Transformer架构中可以优化的点分为两个层面。一个就是对于KV变量的缓存，分别是在编码器和解码器求注意力计算的过程中；另一个是可以应用在注意力激活矩阵中，对于之前的token的矩阵元素的缓存。</p>
<p><strong>注意</strong>，KVCache技术仅在decoder中应用。</p>
<h2>1. Cross-attention KVCache</h2>
<p>首先是交叉注意力部分的KVCache。这部分的注意力要求encoder的输出与decoder中经过MSA的输出进行attention计算。在上图中可以看到，encoder的输出作为decoder中第二个MSA模块的K，V，与decoder第一个MSA模块得到的特征进行cross-attention计算。</p>
<p>在推理阶段，decoder会不断的输出token，不断重复其中的attention计算。而每次计算中K、V是不变的，都来自于encoder的输出。故将此K、V缓存可以提高其推理速度。</p>
<h2>2. Self-attention KVCache</h2>
<p>另一个可以进行KVCache优化的点在decoder中的第一个MSA模块，这一部分直观来看可以称作QKVCache，因为可以将QKV三个变量都进行缓存来降低计算量。在推理阶段，之前预测的token会与当前预测得到的token进行拼接，再次作为decoder的输入，经过decoder计算得到下一个预测token。在这个循环的过程中，decoder的输入首先都会经过MSA计算自注意力。而之前预测得到的token的QKV都是已经计算过的，可以将其缓存，然后在需要计算的时候直接调用。这样模型在每一次decoder的forward过程中，仅需要计算上一次生成的token的QKV即可，然后将其保存便于下一次计算直接调用，提高模型的推理速度。</p>
<h2>3. Final</h2>
<p>这样的KVCache过程仅仅是缓存了输入变量与W_Q, W_K, W_V线性层计算后的embeddings，对于后续QKV之间的计算并没有提出优化，不过顺着transformer的结构和推理流程，attention激活矩阵的部分应该可以再进行缓存优化。</p>
]]></content>
        <author>
            <name>FisherCat</name>
            <email>fishercat_@outlook.com</email>
            <uri>https://inspired-minds.vercel.app</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[Transformer]]></title>
        <id>https://inspired-minds.vercel.app/blog/Transformer</id>
        <link href="https://inspired-minds.vercel.app/blog/Transformer"/>
        <updated>2025-02-27T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[The principle of Transformer]]></summary>
        <content type="html"><![CDATA[<h2>Architecture</h2>
<p><img src="/Transformer/arch.png" alt="Arch"></p>
<h3>1. Out of Transformer: Tokenizer</h3>
<p>训练Transformer需要根据数据集来构建一个词表，为每一个词映射到唯一的Token_ID，表示Transformer理解的词空间。此外，还需要一个Tokenizer(分词器)来将输入的sequence映射为与之对应的ID序列。例如，现有一个简化词表vocab：</p>
<pre><code class="language-python">tokenizer.vocab = {
  &quot;[PAD]&quot;: 0, &quot;[UNK]&quot;: 1, &quot;[BOS]&quot;: 2, &quot;[EOS]&quot;: 3,
  &quot;你&quot;: 10, &quot;好&quot;: 11, &quot;，&quot;: 12, &quot;今天&quot;: 20, &quot;天气&quot;: 21, &quot;如何&quot;: 22, &quot;？&quot;: 23
} # PAD表示补全token的ID，UNK表示不在该词表里的token，BOS表示一个输入序列的开始，EOS表示一个输入序列的结束。
</code></pre>
<p>进行分词并转化为Token_ID 序列：</p>
<blockquote>
<p>&quot;你好，今天天气如何？&quot; $\rightarrow$ [&quot;你&quot;, &quot;好&quot;, &quot;，&quot;, &quot;今天&quot;, &quot;天气&quot;, &quot;如何&quot;, &quot;？&quot;] $\rightarrow$ [10, 11, 12, 20, 21, 22, 23]</p>
</blockquote>
<h3>2. Token Embedding</h3>
<p>在这一部分将输入的Token转化为对应的词向量，作为每个Token的高维特征。</p>
<p>Transformer首先初始化一个<code>nn.Embedding</code>对象并设置其大小以及embedding维度：</p>
<pre><code class="language-python">import torch.nn as nn

vocab_size = 11  # 词表大小
d_model = 512       # 嵌入维度
padding_idx = 0     # 填充符号的索引

# 定义嵌入层（自动处理填充符号的梯度）
token_embedding = nn.Embedding(
    num_embeddings=vocab_size,
    embedding_dim=d_model,
    padding_idx=padding_idx
)
</code></pre>
<p>Token Embedding这一层是需要训练的。在training时，Transformer会将输入的sequence中每一个token按照其相应的token id提取出对应的在 <code>token_embedding</code> 中的词向量(词嵌入)，拼接为当前sequence的输入embedding，来进行后续的计算。</p>
<p>继续上一节的例子，这里输入的Token_ID序列为 <code>[2, 10, 11, 12, 20, 21, 22, 23, 3]</code>。与原始序列相比，增加了BOS和EOS，token_embedding会执行</p>
<pre><code class="language-python">input = token_embedding[Token_ID]
</code></pre>
<p>来选择对应ID的token embedding继续训练。</p>
<h3>3. Positional Embedding</h3>
<p>这一部分对输入的sequence加上一个位置编码，使模型能够理解到token的顺序先后。每一个token都会加一个对应的位置编码，维度与token embedding维度相同。在原始的transformer中，positional embedding是不被训练的，固定的。transformer对输入的sequence的奇数位置和偶数位置分别进行sin编码和cos编码。</p>
<h3>4. Multi-head Self-Attention</h3>
<p>Transformer中的**多头自注意力(Multi-Head Self-Attention)**是模型捕捉序列内部复杂依赖关系的核心机制。它通过并行多个“注意力头”，使模型能够同时关注不同位置和语义层面的信息。</p>
<p><strong>关键优势</strong>：</p>
<ol>
<li>并行性：多个头独立计算注意力，提升计算效率。</li>
<li>多样性：不同头可关注不同模式（如局部依赖、长距离关联、语法角色等）。</li>
</ol>
<p>在完成Token Embedding 和 Positional Embedding操作之后，我们就得到的sequence序列的一个高维特征，形状为 [1, 9, d_model]. 下面我们将进行自注意力来计算词与词之间的关系。首先梳理一下MSA的结构：</p>
<ul>
<li>W_Q, W_K, W_V $\rightarrow$ <strong>QKV 映射矩阵</strong></li>
<li>proj $\rightarrow$ 最终线性变换</li>
</ul>
<pre><code class="language-python">class MultiHeadAttention(nn.Module):
    def __init__(self, d_model, num_heads):
        super().__init__()
        self.d_model = d_model
        self.num_heads = num_heads
        self.d_k = d_model // num_heads

        # 定义线性投影矩阵
        self.W_Q = nn.Linear(d_model, d_model)  # 实际拆分为h个头
        self.W_K = nn.Linear(d_model, d_model)
        self.W_V = nn.Linear(d_model, d_model)
        self.proj = nn.Linear(d_model, d_model)
</code></pre>
<p>MSA 模块就是利用这些矩阵之间的计算得到token与token之间的注意力关系。MSA的计算公式为：
$$
\text{Attention}(Q, K, V) = \text{Softmax}(\frac{QK^\top}{\sqrt{\text{d_k}}})\cdot V
$$
其中，Q、K、V分别代表输入序列$x$分别经过W_Q、W_K、W_V映射后的特征。MSA模块的 <strong>forward</strong> 过程：</p>
<pre><code class="language-python">def forward(self, X, mask=None):
    batch_size, seq_len, _ = X.shape

    # 生成Q/K/V并分头
    Q = self.W_Q(X).view(batch_size, seq_len, self.num_heads, self.d_k).transpose(1, 2)
    K = self.W_K(X).view(batch_size, seq_len, self.num_heads, self.d_k).transpose(1, 2)
    V = self.W_V(X).view(batch_size, seq_len, self.num_heads, self.d_k).transpose(1, 2)

    # 计算注意力分数
    scores = torch.matmul(Q, K.transpose(-2, -1)) / (self.d_k ** 0.5)
    # 应用Mask
    if mask is not None:
        # mask形状需为(batch_size, 1, seq_len, seq_len)或可广播的形状
        scores = scores.masked_fill(mask == 0, -1e9)  # 将mask中0的位置设为 -inf

    attn_weights = F.softmax(scores, dim=-1)

    # 加权求和并合并多头
    output = torch.matmul(attn_weights, V)
    output = output.transpose(1, 2).contiguous().view(batch_size, seq_len, -1)

    # 最终线性变换
    return self.proj(output)
</code></pre>
<p>上面这个过程可以通过这个图更加直观理解：
<img src="/Transformer/MSA.png" alt="Alt text">
在上图中，绿色的向量代表输入的单个token，橙色、粉色、红色向量分别表示当前token经过W_K, W_V, W_Q之后得到的向量，即上述公式中的Q，K，V。之后MSA会求Q与K的点乘，得到每个token与其他token的注意力矩阵(图中蓝色部分)，最终再与V进行计算。</p>
<h4><strong>Attention Mask</strong></h4>
<p>屏蔽无效位置：防止模型关注填充符（Padding Tokens）或未来的信息（解码时避免看到后续词）。例如输入序列 <code>[I, love, NLP, [PAD], [PAD]]</code>，mask标记<code>[1,1,1,0,0]</code>，在MSA计算时会将PAD token得到的值抹去($-\infty$)</p>
<h3>5. Encoder</h3>
<p>编码器部分由N个相同结构的Block组成，每个Block包含：</p>
<ul>
<li>一个MSA模块</li>
<li>一个FFN模块</li>
<li>LayerNorm层</li>
</ul>
<pre><code class="language-python"># PyTorch示例
attn_output = self.attention(x)
x = self.norm1(x + attn_output)  # 残差连接后接层归一化
ffn_output = self.ffn(x)
x = self.norm2(x + ffn_output)
</code></pre>
<p>Encoder接收一个输入句子列表, 即sequence. 在forward的过程中会生成注意力mask</p>
<h3>6. Decoder</h3>
<p>解码器部分由两个多头注意力模块和一个FFN模块构成。</p>
<pre><code class="language-python">class Decoder(nn.Module):
    def __init__(self, d_model, num_head, embd_size):
        self.MSHA1 = MultiHeadAttention(d_model, num_head)
        self.linear1 = nn.Linear(d_model*num_head, embd_size)
        self.norm1 = nn.LayerNorm(embd_size)

        self.MSHA2 = MultiHeadAttention(d_model, num_head)
        self.linear2 = nn.Linear(d_model*num_head, embd_size)
        self.norm2 = nn.LayerNorm(embd_size)

        self.ffn = nn.Sequential(
            nn.Linear(embd_size, d_model),
            nn.ReLU(),
            nn.Linear(d_model, embd_size)
          )
        self.norm3 = nn.LayerNorm(embd_size)

    def forward(self, x, encoder_out, attn_mask_1, attn_mask_2):
        out_1 = self.MSHA1(x, x, attn_mask1)
        out_1 = self.linear1(out_1)
        out_1 = self.norm1(x + out_1)

        out_2 = self.MSHA2(out_1, encoder_out, attn_mask_2)
        out_2 = self.linear2(out_2)
        out_2 = self.norm2(out_2 + out_1)

        return self.norm3(self.ffn(out_2) + out_2)
</code></pre>
<h2>The Training phase of Transformer</h2>
<p>coming soon ...</p>
<h2>The Inference phase of Transformer</h2>
<p>coming soon ...</p>
<h2>Vision Transformer</h2>
<h3>ViT Structure</h3>
<pre><code class="language-python">class VisionTransformer(nn.Module):
    def __init__(self, img_size=224, patch_size=16, in_chans=3, num_classes=1000, global_pool=&quot;token&quot;,
                 embed_dim=768, depth=12, num_heads=12, mlp_ratio=4.0, qkv_bias=True, init_values=None, class_token=True, no_embed_class=False, fc_norm=None, drop_rate=0.0, attn_drop_rate=0.0, drop_path_rate=0.0, weight_init=&quot;&quot;, embed_layer=PatchEmbed, norm_layer=None, act_layer=None, block_fn=Block, prompt_length=None, embedding_key=&quot;cls&quot;, prompt_init=&quot;uniform&quot;, prompt_pool=False, prompt_key=False, pool_size=None, top_k=None, batchwise_prompt=False, prompt_key_init=&quot;uniform&quot;, head_type=&quot;token&quot;, use_prompt_mask=False):
                 ······
</code></pre>
<h3>1. Patch Embed</h3>
<pre><code class="language-python">class PatchEmbed(nn.Module):
    &quot;&quot;&quot; 2D Image to Patch Embedding &quot;&quot;&quot;
    def __init__(self, img_size=224, patch_size=16, in_chans=3, embed_dim=768, norm_layer=None, flatten=True):
        ······
</code></pre>
<p>patch_embed layer主要有两部分构成：</p>
<ul>
<li><code>self.proj = nn.Conv2d(in_chans, embed_dim, kernel_size=patch_size, stride=patch_size)</code></li>
<li><code>self.norm = norm_layer(embed_dim) if norm_layer else nn.Identity()</code></li>
</ul>
<p>在这一层，将$224\times 224$的图像首先通过2维卷积得到高维图像embedding[bz, $N_p$, embed_dim]，记作$x$.  $N_p = (\frac{img_size}{patch_size})^2$, 代表patch的数量，也称做一个token；embed_dim表示高维空间维度。最后进行layer_norm操作，图像embedding形状不变，数值进行归一化。</p>
<h3>2. Positional Embed</h3>
<p><code>self.pos_embed = nn.Parameter(torch.randn(1, embed_len, embed_dim) * 0.02)</code>
位置嵌入初始为一个随机tensor，其形状为 [1, $N_p$+1, embed_dim]. 在ViT中，这个位置编码是可学习的，模型通过训练学到不同patch的位置关系。</p>
<p>在Forward过程中，$x$会与位置编码进行相加，得到带有位置信息的图像embedding，形状不变。</p>
<h3>3. Residual Blocks</h3>
<pre><code class="language-python">class Block(nn.Module):
    def __init__(self, dim, num_heads, mlp_ratio=4.0, qkv_bias=False, drop=0.0, attn_drop=0.0, init_values=None, drop_path=0.0, act_layer=nn.GELU, norm_layer=nn.LayerNorm):
</code></pre>
<p>在这里会执行ViT的大部分计算，ViT模型根据其规模所包含的block数量不同，但每一个block的结构相同。例如，ViT-B包含12个block，ViT-L包含24个block。</p>
<p>每个block包含一个多头自注意力（MSA）和一个多层感知机（MLP）:</p>
<ul>
<li><p>多头自注意力MSA</p>
<pre><code class="language-python">class Attention(nn.Module):
    def __init__(self, dim, num_heads=12, qkv_bias=False, attn_drop=0.0, proj_drop=0.0):
</code></pre>
<p>MSA主要包含: qkv_proj(linear), attn_drop(Dropout), proj(linear)</p>
<p>在MSA的forward过程中，$x$会首先通过qkv_proj层得到qkv特征，分别作为query，key和value，按照如下公式进行计算：
$$
\text{Attention(Q,K,V)} = \text{softmax}(\frac{QK^\top}{\sqrt{d_k}})V
$$
$QK^\top$计算得到所有patch的注意力矩阵（激活图），每个头都计算得到 $\text{num heads}$个注意力矩阵。经过$\text{softmax}$后再与V计算得到注意力特征，赋值给$x$，形状为 [bz, $N_p+1$, dim(768)]. 最后$x$经过<code>proj</code>返回映射后的特征。</p>
</li>
<li><p>多层感知机MLP</p>
<pre><code class="language-python">class Mlp(nn.Module):
  def __init__(self, in_features, hidden_features=None, out_features=None, act_layer=nn.GELU, bias=True, drop=0.):
      ...
      self.fc1 = nn.Linear(in_features, hidden_features, bias=bias[0])
      self.act = act_layer()
      self.drop1 = nn.Dropout(drop_probs[0])
      self.fc2 = nn.Linear(hidden_features, out_features, bias=bias[1])
      self.drop2 = nn.Dropout(drop_probs[1])
      ...
</code></pre>
<p>MLP的结构非常简单，主要由两个线性层和一个激活层构成。这两个线性层可以看作是up_proj和down_proj映射。输入的$x$依次经过<code>fc1, act1, drop1, fc2, drop2</code>，得到映射后的特征，$x$维度不变。</p>
</li>
</ul>
<h3>4. Norm Layer</h3>
<pre><code class="language-python">class LayerNorm(Module):
      def __init__(self, normalized_shape: _shape_t, eps: float = 1e-5, elementwise_affine: bool = True,device=None, dtype=None) -&gt; None:
</code></pre>
<p>层归一化，沿特征维度对输入$x$进行归一化：
$$
\mu = \frac{1}{D}\sum_{i=1}^D x_{b,n,i} \  </p>
<p>\sigma = \sqrt{\frac{1}{D}\sum_{i=1}^D (x_{b,n,i} - \mu)^2} \</p>
<p>y_{b,n,i} = (\frac{x_{b,n,i} - \mu}{\sigma + \epsilon})\cdot \gamma_i + \beta_i
$$
其中，$\gamma$和$\beta$是可学习参数，$\epsilon$是为数值稳定性添加的小常数（如1e-5）。</p>
]]></content>
        <author>
            <name>FisherCat</name>
            <email>fishercat_@outlook.com</email>
            <uri>https://inspired-minds.vercel.app</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[InfoNCE Loss Explanation]]></title>
        <id>https://inspired-minds.vercel.app/blog/info_nce_loss</id>
        <link href="https://inspired-minds.vercel.app/blog/info_nce_loss"/>
        <updated>2024-12-19T00:00:00.000Z</updated>
        <content type="html"><![CDATA[<h2>对比学习和自监督学习中的InfoNCE损失</h2>
<p>InfoNCE（Noise Contrastive Estimation）和交叉熵损失都是两个关键的概念。它们不仅在衡量概率分布之间的差异方面发挥着重要作用，而且在深度学习的自监督学习领域扮演着重要角色。虽然它们的形式和应用环境有所不同，但是我们可以发现它们之间存在着微妙的联系。</p>
<p>InfoNCE Loss（Noise Contrastive Estimation Loss）是一种用于自监督学习的损失函数，通常用于学习特征表示或者表征学习。它基于信息论的思想，通过对比正样本和负样本的相似性来学习模型参数。</p>
<h2>InfoNCE Loss 公式</h2>
<p>InfoNCE 损失的计算公式如下：
$$
\mathcal{L}<em>{NCE} = -\frac{1}{N} \sum</em>{i=1}^N \log\frac{\exp(P_{i, pos}/\tau)}{\sum_k \exp(P_{i,k}/\tau)}
$$</p>
<p>其中$P_{i,pos}$表示第i个样本与其正样本的相似性/距离，log右侧整体为正样本的概率分布。</p>
<p>CLIP中用到的对比损失就是典型的InfoNCE Loss，其正样本对在相似性矩阵的对角线上。
<img src="/InfoNCE_Loss/QQ_1734618945685.png" alt="Alt text">
<img src="/InfoNCE_Loss/QQ_1734618971678.png" alt="Alt text"></p>
<p>InfoNCE损失在自监督学习场景下也发挥着重要的作用。在此以多视角自监督对比学习举例。</p>
<p>对于一个批次的数据$X\in \mathbb{R}^{n\times c\times w\times h}$，我们计算其两个视角的图像特征$F,F&#39;$。随后计算相似性矩阵$S = F\cdot F^T$，其中$F \in \mathbb{R}^{n\times d}$。</p>
<p>在$S$中，对于一个样本数据 $x_i$，其正样本为它的第二个视角数据 $x_i&#39;$ 以及其自身。得到$S$之后，我们需要对其进行修改，抹去对角线元素，拉近样本与其第二个视角数据的相似性。</p>
<p><img src="/InfoNCE_Loss/QQ_1734620303984.png" alt="Alt text"></p>
<p>在上图所示的相似性矩阵中，左上角表示view_1的数据与view_1的数据的相似性矩阵，右上角表示view_1的数据与view_2的数据的相似性矩阵，其他以此类推。数字0，512，511表示$S$的维度。</p>
<p>然后我们从中筛选出正样本对的相似性向量$pos \in \mathbb{R}^{n\times1}$，负样本对的相似性向量$neg = \in \mathbb{R}^{n\times (n-2)}$，构成概率分布 $\text{logits} = [pos, neg] \in \mathbb{R}^{n\times (n-1)}$。因为正样本设置在logits开头处，我们构造标签为长度为 n 的向量$y \in \mathbb{R}^n$，其中所有元素为0，计算logits与y的CE损失即可得到目标损失。</p>
<h2>具体代码实现</h2>
<pre><code class="language-python">def info_nce_logits(features, batch_size, n_views=2, temperature=1.0):
    &quot;&quot;&quot;
    It is assumed that features are aggregated so the first of all the images is first,
    then the second view of all images and so on
    So e.g. for args.n_views == 3, features = [x_1, x_2, ..., x_1&#39;, x_2&#39;, ...,  x_1&#39;&#39;, x_2&#39;&#39;, ...]
    &quot;&quot;&quot;
    device = features.device
    labels = torch.eye(batch_size, dtype=torch.bool, device=device).repeat(n_views, n_views)
    # labels is a correspondence matrix: do the features come from the same image?

    features = F.normalize(features, dim=1)
    similarity_matrix = torch.matmul(features, features.T)

    # discard the main diagonal from both: labels and similarities matrix
    mask = ~torch.eye(labels.shape[0], dtype=torch.bool, device=device) # False on the diagonal, True elsewhere
    labels = labels[mask].view(labels.shape[0], -1)
    similarity_matrix = similarity_matrix[mask].view(similarity_matrix.shape[0], -1)

    # select and combine multiple positives
    positives = similarity_matrix[labels].view(labels.shape[0], -1)

    # select only the negatives the negatives
    negatives = similarity_matrix[~labels].view(labels.shape[0], -1)

    logits = torch.cat([positives, negatives], dim=1)
    labels = torch.zeros(logits.shape[0], dtype=torch.long, device=device)

    logits = logits / temperature
    return logits, labels
</code></pre>
<h2>Reference</h2>
<p><a href="https://github.com/grypesc/CAMP/blob/main/utils/losses.py">Original Repo of Code</a></p>
<p><a href="https://blog.csdn.net/weixin_43427721/article/details/134539003">InfoNCE Loss公式及源码理解</a></p>
]]></content>
        <author>
            <name>FisherCat</name>
            <email>fishercat_@outlook.com</email>
            <uri>https://inspired-minds.vercel.app</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[Git Technic]]></title>
        <id>https://inspired-minds.vercel.app/blog/Git</id>
        <link href="https://inspired-minds.vercel.app/blog/Git"/>
        <updated>2024-12-17T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[Threads of Git]]></summary>
        <content type="html"><![CDATA[<h2>Git 常用命令</h2>
<h2>配置用户</h2>
<pre><code class="language-bash"> git config --global/local user.name &quot;FisherCat&quot;

 git config --global/local user.email fishercat_@outlook.com
</code></pre>
<p>global/local表示的是对全局仓库有效还是仅对当前本地仓库有效</p>
<pre><code class="language-bash"> git config --global credential.helper store
 保存信息下次登录时无需再次输入

  git config --global --list
</code></pre>
<p>查看配置信息</p>
<h2>创建仓库</h2>
<ol>
<li>git init  从本地创建仓库<ul>
<li>git status 查看仓库状态</li>
<li>git add  添加到暂存区<ul>
<li>git rm --cached <code>file</code> 将已添加到暂存区的文件撤回</li>
<li>git add . 将当前文件夹下的所有文件都添加到暂存区中</li>
</ul>
</li>
<li>git commit 提交（只会提交<strong>暂存区</strong>中的文件）<ul>
<li>参数&quot;-m&quot;：添加提交备注</li>
</ul>
</li>
</ul>
</li>
<li>git clone  从服务器上拷贝仓库</li>
<li>git log  查看当前仓库提交记录<ul>
<li>参数“--oneline” 显示简略的提交记录</li>
</ul>
</li>
<li>git ls-files  查看暂存区文件</li>
<li>git reflog  查看历史记录</li>
<li>git diff  查看工作区、暂存区、版本仓库之间的差异<ul>
<li>git diff  比较当前工作区和暂存区之间的差异</li>
<li>git diff HEAD 比较工作区和版本仓库之间的差异</li>
<li>git diff --cached 比较工作区和暂存区的差异</li>
<li>git diff 版本ID1 版本ID2 比较两个提交版本之间的差异,版本2相较于版本1做出的更改<blockquote>
<p>HEAD 指向分支的最新提交版本，HEAD～或HEAD^表示上一个提交版本。HEAD～2表示HEAD之前的两个版本</p>
</blockquote>
</li>
</ul>
</li>
<li>git rm <code>文件名</code>  同时在工作区和暂存区中删除文件<ul>
<li>git rm -r *  递归删除某个目录下的所有子目录和文件，删除后记得要提交</li>
<li>git rm --cached <code>文件名</code>  把文件从暂存区中删除，但是在工作区中仍然保存</li>
</ul>
</li>
</ol>
<h2>Git本地数据管理</h2>
<ol>
<li>工作区
 本地文件目录 &quot;.git/&quot;</li>
<li>暂存区 Staging Area/Index
 临时存储区，用于保存即将提交到Git仓库的修改内容 &quot;.git/index/&quot;</li>
<li>本地仓库
 使用git init/clone创建的仓库 .git/objects/；是Git存储代码和版本信息的主要位置
<img src="/Git/git_procedure.png" alt="Img"></li>
</ol>
<h2>Git中的文件状态</h2>
<ol>
<li>未跟踪: 刚刚创建还未被git管理</li>
<li>未修改: 已经由git管理但是文件的内容还未变化</li>
<li>已修改: 已经修改的文件，但是尚未被添加到暂存区</li>
<li>已暂存: 已经修改的文件已添加到暂存区</li>
</ol>
<h2>Git Reset 回退</h2>
<ul>
<li>git reset --soft<ul>
<li>回退到某一个版本，并保存工作区和暂存区的内容</li>
</ul>
</li>
<li>git reset --hard<ul>
<li>回退到某一个版本，并丢弃工作区和暂存区的内容</li>
</ul>
</li>
<li>git reset --mixed （默认）<ul>
<li>回退到某一个版本，只保留工作区的内容</li>
</ul>
</li>
</ul>
<h2>.gitignore文件</h2>
<p>版本库中不应保留的文件：</p>
<ol>
<li>系统或者软件自动生成的文件</li>
<li>编译产生的中间件和结果文件</li>
<li>运行时生成的日志文件、缓存文件、临时文件等</li>
<li>涉及身份、密码、口令、密钥等敏感信息的文件</li>
</ol>
<ul>
<li>将文件名写入.gitignore文件中即可忽略提交（目录要在之后加‘/’）
忽略前提：这个文件不能是已经被加入到仓库中的文件
空文件夹也不会被加入到仓库中</li>
<li>.gitignore文件匹配规则<ul>
<li>从上到下逐行匹配，每一行表示一个忽略模式</li>
<li>空行或者以‘#’开头的行会被Git忽略，#用于注释</li>
<li>模糊匹配规则</li>
</ul>
</li>
<li>示例<ol>
<li>只忽略当前目录下的TODO文件：/TODO</li>
<li>忽略任何目录下名为build的文件夹：build/
常用语言忽略模版：<a href="https://github.com/github/gitignore">link</a></li>
</ol>
</li>
</ul>
<h2>远程仓库</h2>
<ol>
<li>git pull <code>remote</code>  拉取远程仓库，同时同步本地和远程仓库的修改。git fetch只获取远程仓库的修改内容。</li>
<li>git push <code>remote</code> <code>branch</code>  推送本地仓库
 git push -u origin main:main 将本地仓库与远程仓库origin关联，将本地仓库的main分支推送到远程仓库的main分支</li>
<li>git remote add <code>别名</code> <code>url</code>  添加一个远程仓库</li>
<li>git remote -v 查看远程仓库信息</li>
</ol>
]]></content>
        <author>
            <name>FisherCat</name>
            <email>fishercat_@outlook.com</email>
            <uri>https://inspired-minds.vercel.app</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[Covariance Matrix Explanation]]></title>
        <id>https://inspired-minds.vercel.app/blog/Cov_Matrix</id>
        <link href="https://inspired-minds.vercel.app/blog/Cov_Matrix"/>
        <updated>2024-11-07T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[Threads of covariance matrix]]></summary>
        <content type="html"><![CDATA[<p><img src="/Cov_Matrix/image.png" alt="Alt text">
<img src="/Cov_Matrix/image-1.png" alt="Alt text">
<img src="/Cov_Matrix/image-2.png" alt="Alt text">
<img src="/Cov_Matrix/image-3.png" alt="Alt text">
<img src="/Cov_Matrix/image-4.png" alt="Alt text">
<img src="/Cov_Matrix/image-6.png" alt="Alt text"></p>
]]></content>
        <author>
            <name>FisherCat</name>
            <email>fishercat_@outlook.com</email>
            <uri>https://inspired-minds.vercel.app</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[MCPNet,An Interpretable Classifier via Multi-Level Concept Prototypes]]></title>
        <id>https://inspired-minds.vercel.app/blog/MCPnet</id>
        <link href="https://inspired-minds.vercel.app/blog/MCPnet"/>
        <updated>2024-06-10T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[CVPR2024 | It propose a new method for classification, by calculating the similarity of multi-level distribution between instance and prototype.]]></summary>
        <content type="html"><![CDATA[<blockquote>
<p><a href="http://arxiv.org/abs/2404.08968">原文</a></p>
</blockquote>
<h2>Core idea</h2>
<p>作者认为用通过backbone得到的特征来做原型太单一，遂决定提取通过不同backbone层数后特征做原型，设置“原型强度”指标，来查看不同层数原型的特点，以此作为原型分布，并通过比图像通过backbone得到的多层数原型分布来完成分类任务</p>
<h2>Work flow</h2>
<p><img src="/MCPNet/image.png" alt="work flow">
在训练阶段，我们计算每个训练图像的MCP分布，最后将其平均，得到每个类的原型分布并保存；在测试阶段，想要分类一张图像，我们首先计算他的<strong>MCP</strong>分布，然后将其MCP分布与所有的原型分布比较(JS散度)完成分类。</p>
<h2>Training flow</h2>
<h3>Algorithm</h3>
<p><img src="/MCPNet/image-7.png" alt="Algo"></p>
<h3>CKA Similarity</h3>
<p>在训练backbone的同时，提取得到目标类的多层原型，CKA(Centered Kernel Alignment) 旨在使得到的原型更加多样更具辨别性<br><img src="/MCPNet/image-1.png" alt="CKA"><br>$H$代表无偏希尔伯特-施密特独立性准则，$H(X,Y):$<br><img src="/MCPNet/image-2.png" alt="Alt text"><br>CKA loss:<br>$$\mathcal{L}^{CKA}(S_l) = \frac{2}{M_l(M_l-1)}\sum^{M_l}<em>{i=1}\sum^{M_l}</em>{j=1}\bold{CKA}(S_{l,i},S_{l,j})$$
其中$S_{l,i}$表示第l曾第i个概念切片，$M_l$代表第l曾的概念切片总数。    </p>
<h3>CCD loss</h3>
<p>计算样本的多层概念分布，与原型的概念分布进行JS散度计算，使得样本的分布尽量靠近类原型的分布，提高分类精度。m是一个超参数，当样本与其不同的类原型的分布相似时，应加大损失。     </p>
<p><img src="/MCPNet/image-3.png" alt="CCD loss"></p>
<p>综上所述，该方法的总损失为：<br>$$\mathcal{L} = \sum^L_{l=1}\mathcal{L}^{CKA}(S_l) + \lambda_{CCD}\sum_{x_i \in X}\mathcal{L}^{CCD}(x_i)$$</p>
<h3>Test flow</h3>
<p>测试时与<a href="../prototypical-network/">Prototypical Network</a> 相似，也是计算与原型的相似度（距离），完成分类，可解释性更强。</p>
<h2>Experiments</h2>
<h3>Main results</h3>
<p><img src="/MCPNet/image-4.png" alt="exp"></p>
<h3>原型概念切片</h3>
<p><img src="/MCPNet/image-5.png" alt="Alt text"></p>
<p>此外，基于原型的方法相比于其他方法在小样本领域更加有效。<br><img src="/MCPNet/image-6.png" alt="Alt text"></p>
<h3>Ablation Study</h3>
<p><img src="/MCPNet/image-8.png" alt="Alt text">
作者对于提出的两个方法也进行了消融对比，CCD loss的相对提升很大。</p>
]]></content>
        <author>
            <name>FisherCat</name>
            <email>fishercat_@outlook.com</email>
            <uri>https://inspired-minds.vercel.app</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[iCarL]]></title>
        <id>https://inspired-minds.vercel.app/blog/icarl</id>
        <link href="https://inspired-minds.vercel.app/blog/icarl"/>
        <updated>2024-06-10T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[CVPR2017 | Replay data, NME, KD & Prototipical.]]></summary>
        <content type="html"><![CDATA[<blockquote>
<p><a href="https://arxiv.org/abs/1611.07725">原文</a></p>
</blockquote>
<h2>作者定义的类增量学习算法的三种特性</h2>
<ol>
<li>它是可以在一个数据流中学习，而且数据流中的不同类别的样本可以出现在不同的时刻。</li>
<li>它应当在任何时刻都能够提供一个卓有成效的分类器，能够对已经观察到的类进行分类。</li>
<li>对已经“看”到的类别而言，它的计算能力和内存占用应该是有限的，至少是非常缓慢的增长。</li>
</ol>
<h2>iCarL构成</h2>
<ul>
<li>基于<strong>最近样本均值</strong>（<em>nearest-mean-of-exemplars</em>）法的分类器</li>
<li>基于<strong>集群</strong>（<em>herding</em>）方法的样本优先选择</li>
<li>基于<strong>知识蒸馏</strong>和<strong>原型彩排</strong>（<em>prototype rehearsal</em>）的表征学习</li>
</ul>
<h2>iCarL 算法</h2>
<p>在每一次新类到达时，iCarL都要进行更新，调整网络的内部参数和样本。</p>
<p><img src="/iCarL/image.png" alt="Alt text"></p>
<ul>
<li>iCarL 首先利用当前的样本和数据，进行网络参数的更新</li>
<li>进而计算当前内存所能容纳的样本数量，计算样本容量</li>
<li>接下来是一个循环，对所有的类别进行样本缩减。</li>
<li>之后是另一个循环，重新构筑每个类的样本，得到新的样本集合</li>
</ul>
<h3>最近样本均值分类（nearest-mean-of-exemplars）</h3>
<p>为预测一个新的图片$x$的类别$y^*$，iCarL为一观察到的每个类计算一个原型向量（<em>prototype vector</em>），${\mu_1,\mu_2...,\mu_t}$，原型向量的计算公式为：$\mu_y = \frac{1}{|P_y|}\sum_{p\in P_y}\varphi(p)$，所以$\mu_y$即为类别y的所有样本的均值特征向量。</p>
<blockquote>
<p>$P_y$表示y类的样本集合，p代表某个样本。</p>
</blockquote>
<p>以此原型向量来进行分类：$y^* = argmin_{y={1...t}}||\varphi(x)-\mu_y||$，最小化均方误差。</p>
<p><img src="/iCarL/image-1.png" alt="Alt text"></p>
<p>在iCarL中，不能计算真正的类均值，因为这样需要存储所有的类数据，与增量学习原则相悖。</p>
<blockquote>
<p>$y^<em>$也可以写成$y^</em> = argmax_y\mu_y^\top\varphi(x)$。</p>
</blockquote>
<h3>更新表征的算法（<em>update representation</em>）</h3>
<p><img src="/iCarL/image-2.png" alt="Alt text"></p>
<p>当iCarL遇到新的类别时，就要启动更新算法。</p>
<ul>
<li><p>首先，iCarL会构建一个新的训练集合，由当前存储的样本和新的数据构成。</p>
</li>
<li><p>接下来，iCarL需要将它的网络参数保存，以便于进行之后的优化（<em>知识蒸馏</em>）。</p>
</li>
<li><p>然后利用新的训练集合来训练网络，最小化损失函数。</p>
<p>对于损失函数$\ell(\Theta)$，第一个部分是计算新的类别的交叉熵，来学习分辨新的类；而第二个部分是利用知识蒸馏来巩固之前学习的知识（<em>network parameters</em>），防止遗忘。</p>
</li>
</ul>
<blockquote>
<p>作者通过对数据集进行扩充以及对loss function进行“扩充”来减缓灾难性遗忘。</p>
</blockquote>
<h3>样本集构建算法</h3>
<p><img src="/iCarL/image-3.png" alt="Alt text"></p>
<p>作者对样本集的重构算法。将原样本集合缩减到m个样本。</p>
<ul>
<li><p>首先计算数据集$X$的特征均值$\mu$</p>
</li>
<li><p>然后循环m次计算m个新的样本，minimize loss function。</p>
<p>该function的目的是求距离$\mu$ 最近的样本特征，选择使得该function最小的$x$，作为新的样本$p_k$。</p>
</li>
<li><p>然后将得到的样本集合为新样本集（exemplar sets）。</p>
</li>
</ul>
<h3>样本集缩减算法</h3>
<p><img src="/iCarL/image-4.png" alt="Alt text"></p>
<ul>
<li><p>样本集缩减算法比较简单，选定常数m，直接从原样本集中选择前m个样本即可。</p>
<p>因为在构建样本集的时候，算法是得到的样本集是有优先级的（<em>prioritized</em>）。</p>
</li>
</ul>
<h2>iCarL 网络架构</h2>
<p>iCarL采用了经典深度学习的CNN网络结构，最后加上一个单层分类层，由sigmoid函数作为激活函数，对已经发现的类别进行分类。iCarL仅仅利用网络进行表征学习，并不用于分类。</p>
<p>习的CNN网络结构，最后加上一个单层分类层，由sigmoid函数作为激活函数，对已经发现的类别进行分类。iCarL仅仅利用网络进行表征学习，并不用于分类。</p>
]]></content>
        <author>
            <name>FisherCat</name>
            <email>fishercat_@outlook.com</email>
            <uri>https://inspired-minds.vercel.app</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[Math Concepts markup]]></title>
        <id>https://inspired-minds.vercel.app/blog/math_concept_markup</id>
        <link href="https://inspired-minds.vercel.app/blog/math_concept_markup"/>
        <updated>2024-06-05T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[Some explaination of math concepts, such as cross entropy, KL divergence, Softmax, and this post will be updated when some new concepts trick me.]]></summary>
        <content type="html"><![CDATA[<h2>信息熵：</h2>
<p>$H(x) = \sum_{k=1}^{n}p_klog_2p_k$</p>
<h2>KL散度：</h2>
<p>相对熵 = KL散度
KL散度：两个概率分布间的非对称性度量
KL散度就是用来衡量同一个随机变量两个不同分布间的<strong>距离</strong>。
$$
D_{KL}(p||q) = \sum_{i=1}^{n}p(x_i)log(\frac{p(x_i)}{q(x_i)})
$$
由此可见，KL散度必 &gt; 0；</p>
<h3>KL散度的特性</h3>
<ol>
<li>非对称性
 $D_{KL}(p||q) \neq D_{KL}(q||p)$；P和Q的概率分布完全一样才相等。</li>
<li>非负性
 $D_{KL}$恒大于等于0。</li>
</ol>
<h3>KL散度公式变形</h3>
<p>$D_{KL} =  \sum_{i=1}^{n}p(x_i)\log(\frac{p(x_i)}{q(x_i)}) \
 =\sum_{i=1}^{n}p(x_i)\log p(x_i)-\sum_{i=1}^{n}p(x_i)\log q(x_i)\
 =-(-\sum_{i=1}^{n}p(x_i)\log p(x_i))-\sum_{i=1}^{n}p(x_i)\log q(x_i)\
 =H(P,Q)-H(P)\
 =交叉熵-信息熵$</p>
<h2>Jensen-Shannon散度</h2>
<p>JS散度度量了两个概率分布的相似度，基于KL散度的变体，解决了KL散度非对称的问。一般地，JS散度是对称的，其取值是0到1之间。定义如下：<br>$$M = \frac{P+Q}{2}$$
$$JS(P||Q) = \frac{1}{2}KL(P||M) + \frac{1}{2}KL(Q||M)$$
KL散度和JS散度度量的时候有一个问题：   </p>
<blockquote>
<p>如果两个分配P,Q离得很远，完全没有重叠的时候，那么KL散度值是没有意义的，而JS散度值是一个常数。这在学习算法中是比较致命的，这就意味这这一点的梯度为0。梯度消失了。</p>
</blockquote>
<h2>交叉熵</h2>
<p>交叉熵应用：主要用于度量同一个随机变量X的预测分布Q与真实分布P之间的<strong>差距</strong>。
预测越准确，交叉熵越小；
交叉熵只和真实标签的预测概率值有关。</p>
<h3>交叉熵的最简公式</h3>
<p>$Cross\space entropy(p,q) = -\log q(c_i)$
$q(c_i)$为真实标签为1 的对应类别的模型预测值。</p>
<h3>交叉熵二分类公式</h3>
<p>$H(P,Q) = -\sum_{i=1}^np(x_i)\log q(x_i)\
=-(p(x_1)\log q(x_1)+p(x_2)\log q(x_2))\
=-(p\log q+(1-p)\log (1-q))$</p>
<p>$p(x_1)+p(x_2) = 1;设p(x_1) = p \
q(x_1)+q(x_2) = 1;设q(x_1) = q$</p>
<h2>Mahalanobis 距离</h2>
<p><a href="https://blog.csdn.net/Bit_Coders/article/details/115859264">reference from 虾米小馄炖</a>   </p>
<p>当需要度量点（向量）与多变量分布之间的距离时，如果直接采用欧式距离，衡量的是两点之间的直接距离（点与分布之间的欧式距离，指的是向量x与变量空间中心的距离），而没有考虑数据的分布特性。</p>
<p>而采用马氏距离，在计算中对协方差进行归一化，则可以规避欧式距离对于数据特征方差不同的风险，从而使所谓的“距离”更加符合数据分布特征以及实际意义。
下面举一个具体例子，说明欧式距离在多变量距离度量方面的局限性。</p>
<p>当我们想要对多变量数据使用欧式距离，可能会遇到一个常见的问题：如果多变量（比如数据集中的列）之间相互关联（这在实际数据集中通常是这种情况），则点与点中心（分布）之间的欧几里得距离会具有误导性，无法真实反映一个点与分布之间的实际距离。</p>
<p>如下图所示，这是是两个变量的简单散点图。左图是两个变量之间不相关，Point 1和Point 2与分布中心的距离相等。右图是两个变量之间呈正相关。即随着一个变量（x轴）的值增加，另一变量（y轴）的值也增加。
<img src="/Math_Concepts_markup/image-5.png" alt="Alt text">
从几何上说，Point 1和Point 2两个点与分布中心的距离相等（欧几里得距离）。但是，即两点到分布的欧几里得距离相等，但实际上只有Point 1（蓝色）更接近该分布。</p>
<p>这是因为，欧几里得距离仅是两点之间的距离，它不考虑数据集中的其余点的分布情况。因此，它不能用来真正判断一个点实际上与点的分布有多接近。所以我们需要的是更健壮的距离度量标准，该度量标准可以精确地表示一个点与分布之间的距离。</p>
<h3>马氏距离（Mahalanobis Distance）：</h3>
<p>$$
D_{mahal} = \sqrt{(x-\mu)\Sigma^{-1}(x-\mu)}
$$</p>
<p>这实际上是多元变量的常规标准化 $z =\frac{(x – \mu)}{\sigma}$。也就是说，z =（x向量）–（平均向量）/（协方差矩阵）。</p>
<p>如果数据集中的变量高度相关，则协方差将很高。除以较大的协方差将有效缩短距离。同样，如果X不相关，则协方差也不高，距离也不会减少太多。</p>
<h3>为什么在很多的网络模型中,使用交叉嫡做损失函数而不使用KL散度做损失函数呢?</h3>
<p>因为有真实分布，若没有真实数据分布，需要用KL散度。</p>
<h2>Softmax？</h2>
<p>softmax是将数字转化成概率神器，进行数据归一化的利器。
$$
S_i = \frac{e^{z_i}}{\sum_{i=1}^ne^{z_i}}
$$
<img src="/Math_Concepts_markup/image.png" alt="Alt text"></p>
<h2>Cross Entropy loss() 函数</h2>
<p>$$loss(x,class) = -\log(\frac{e^{x[class]}}{\sum_j e^{x[j]}}) = -\log e^{x[class]}+\log (\sum_j e^{x[j]})$$
X:模型上各分类输出的具体值；class是真实标签。</p>
<p>第一步:算出真实标签对应数值所占的概率。
第二步:求-log(真实标签对应数值所占的概率)  </p>
<h2>Fisher信息矩阵</h2>
<p>Fisher信息矩阵是衡量一个分布$f(x;\theta)$中随机变量X所携带的信息量的多少。其中的$\theta$并不知道，需要近似估计。
样本$X = {X_1,X_2,X_3...X_n}$所携带的信息量越多，我们越能够准确的近似估计$\theta$的值，越趋近于真实分布。</p>
<h3>Score Function</h3>
<p>$$S(X;\theta) = \sum_{i=1}^n\frac{\partial\log f(X_i;\theta)}{\partial \theta}$$</p>
<h3>Fisher Information</h3>
<p>$$I(\theta) = E[S(X;\theta)^2]$$
Fisher信息量即为Score Function的二阶矩。由于$E[S(X;\theta)] = 0$，所以$I(\theta)$ = $Var[S(X;\theta)]$，即S的协方差矩阵。</p>
<h3>$E[S(X;\theta)] = 0$的证明</h3>
<p>$$
\begin{array}{l}
\underset{p(x \mid \theta)}{\mathbb{E}}[\nabla \log p(x \mid \theta)] \ \
=\int \nabla \log p(x \mid \theta) p(x \mid \theta) \mathrm{d} x \  \
=\int \frac{\nabla p(x \mid \theta)}{p(x \mid \theta)} p(x \mid \theta) \mathrm{d} x \ \
=\int \nabla p(x \mid \theta) \mathrm{d} x \ \
=\nabla \int p(x \mid \theta) \mathrm{d} x \ \
=\nabla 1 \ \
=0
\end{array}
$$</p>
<h2>Regularization</h2>
<p>$$L(W)=\frac{1}{N} \sum_{i=1}^{N} L_{i}\left(f\left(x_{i}, W\right), y_{i}\right)+\lambda R(W)$$
L 是我们的损失函数，其中的第二项是正则化项。λ表示正则化强度。</p>
<ul>
<li>L2 Regularization : $R(W)=\sum_{k} \sum_{l} W_{k, l}^{2}$，对于网络中的权重取平方再求和作为正则项函数R（W）</li>
<li>L1 Regularization：$R(W)=\sum_{k} \sum_{l} |W_{k, l}|$，对于网络中的权重取绝对值再求和作为正则项函数R（W）</li>
<li>Elastic Net（L1+L2）：$R(W)=\sum_{k} \sum_{l} |W_{k, l}|+\beta W_{k,l}^2$</li>
</ul>
<p>正则化能够避免我们的模型在训练的过程中过拟合。</p>
<h3>Dropout</h3>
<p>Dropout是模型训练中的一种策略，能够显著的缓解模型的过拟合现象。Dropout在模型的训练过程中，通过随机的概率使得某些（一般是一半）的神经元停止工作，将训练的结果与其相关的权重关系减少。</p>
<p>在2012年，Hinton在其论文《Improving neural networks by preventing co-adaptation of feature detectors》中提出Dropout。当一个复杂的前馈神经网络被训练在小的数据集时，容易造成过拟合。为了防止过拟合，可以通过阻止特征检测器的共同作用来提高神经网络的性能。同年，Alex、Hinton在其论文《ImageNet Classification with Deep Convolutional Neural Networks》中用到了Dropout算法，用于防止过拟合。并且，这篇论文提到的AlexNet网络模型引爆了神经网络应用热潮，并赢得了2012年图像识别大赛冠军，使得CNN成为图像分类上的核心算法模型。<a href="https://zhuanlan.zhihu.com/p/38200980">inference zhihu</a>
<img src="/Math_Concepts_markup/image-1.png" alt="Alt text">
<strong>利用Dropout的训练流程：</strong></p>
<ol>
<li>在训练中，首先使一部分神经元以一定概率停止工作。</li>
<li>然后利用剩余的神经元进行训练，并利用BP进行权重参数更新。</li>
<li>在本轮训练结束后，回复被停止工作的神经元，再重复上述过程。</li>
</ol>
<p><strong>Dropout网络训练流程：</strong>
参考知乎作者文章  <a href="https://zhuanlan.zhihu.com/p/38200980">Microstrong</a>
在训练过程中添加一道概率流程：
<img src="/Math_Concepts_markup/image-2.png" alt="Alt text">
图中第一个网络为常规的神经网络，第二个网络为采用Dropout策略的神经网络。
其中 $r_i^{(l)}$ 为概率算子。$r_i^{(l)} = Bernolli(p)$，利用伯努利函数随机生成0，1向量。</p>
<blockquote>
<p><strong>ATTENTION</strong>：对上述的神经元停止其工作后，需要放缩剩余的权重大小，即$w_i = w_i * \frac{1}{1-p}$以平衡权重的损失。p为神经元被屏蔽的概率</p>
</blockquote>
<h3>Batch Normalization</h3>
<p>来自知乎用户<a href="https://www.zhihu.com/question/24529483/answer/114711446">知乎用户TXy6or</a>的评论：</p>
<p>就是说，没有BN的时候，模型初始权重值的变化会非常影响梯度下降的结果，因为在前向传播的过程中，激活值的分布会发生变化，由一开始的标准正态分布逐渐发生偏移，也就是internal covariate shift内部协方差移位，均值不再是0，方差也不再是1。而BN就是对于每一层的输入值都进行normalization，通过将每个值减去当前分布的均值，再除以标准差，重新得到标准正态分布，在经过一次仿射变换，既保留了上一层学习的结果，又让分布与一开始的分布偏差没有那么大，因此不用那么小心的选择初始值了，模型的健壮性变得更强。</p>
<h3>Stochastic deepth, fractional pooling</h3>
<h3>OneHot Encoder</h3>
<p>用N个比特来表示N个特征，二进制，每个编码串中只有一位为1.
优点：独热编码不会受到数值编码中编码大小的影响
缺点：如果编码项过多，储存和计算代价将陡增
示例数据：
<img src="/Math_Concepts_markup/image-3.png" alt="Alt text">
独热编码结果：
<img src="/Math_Concepts_markup/image-4.png" alt="Alt text"></p>
<h3>Lp范数</h3>
<p>公式：$||x||_p = \sqrt[p]{\sum_i|x_i|^p}$，表示向量$x$的Lp范数。
L0范数：表示向量x中非0元素的个数。
L1范数：表示向量x中所有元素绝对值之和
L2范数：表示向量大小（模长）</p>
<h3>Weight Decay</h3>
<p>摘自知乎：<a href="https://www.zhihu.com/question/24529483/answer/114711446">autocyz</a></p>
<p>其最终目的是防止过拟合。在损失函数中，weight decay是放在正则项（regularization）前面的一个系数，正则项一般指示模型的复杂度，所以weight decay的作用是调节模型复杂度对损失函数的影响，若weight decay很大，则复杂的模型损失函数的值也就大。</p>
<h3>Momentum</h3>
<p>摘自知乎：<a href="https://www.zhihu.com/question/24529483/answer/114711446">autocyz</a></p>
<p>momentum是梯度下降法中一种常用的加速技术。对于一般的SGD而言，$x = x - \alpha  · dx$。而采用Momentum的梯度下降公式表示为：
$v = \beta v - \alpha v$，$x = x + v$
其中$\beta$即momentum系数，通俗的理解上面式子就是，如果上一次的momentum（即
$v$）与这一次的负梯度方向是相同的，那这次下降的幅度就会加大，所以这样做能够达到加速收敛的过程。</p>
<h3>Scheduler</h3>
<p>Scheduler根据训练的轮次对学习率进行调整。Pytorch源代码中的注释：</p>
<pre><code class="language-Python">class MultiStepLR(LRScheduler,milestone,gamma):
Decays the learning rate of each parameter group by gamma once the number of epoch reaches
one of the milestones. Notice that such decay can happen simultaneously with other changes to
the learning rate from outside this scheduler. When last_ epoch=-1, sets initial Ir as Ir.
</code></pre>
]]></content>
        <author>
            <name>FisherCat</name>
            <email>fishercat_@outlook.com</email>
            <uri>https://inspired-minds.vercel.app</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[Expandable Subspace Ensemble for Pre-Trained Model-Based Class-Incremental Learning]]></title>
        <id>https://inspired-minds.vercel.app/blog/EASE</id>
        <link href="https://inspired-minds.vercel.app/blog/EASE"/>
        <updated>2024-05-27T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[CVPR2024 | Every task train an adapter, and utilize prototypical classifier to keep knowledge.]]></summary>
        <content type="html"><![CDATA[<blockquote>
<p><a href="http://arxiv.org/abs/2403.12030">原文</a></p>
</blockquote>
<h2>Core idea</h2>
<ol>
<li>每个任务训练一个Adapter，使得学习新任务时几乎不会对旧任务的知识产生影响。</li>
<li>利用原型分类器进行分类，可以很好的利用每个Adapter的知识。</li>
<li>利用prototype恢复方法拟合旧任务的数据经过新任务的Adapter所得到的原型。</li>
</ol>
<h2>Overview</h2>
<p><img src="/EASE/featured.png" alt="Alt text"></p>
<h2>原型恢复方法</h2>
<p>作者认为不同的类别语义上的相关性可以跨越不同的特征空间，可以基于重用相似类别的原型生成其相关类别的原型。具体来说，作者的方法是测量旧子空间（所有类同时出现）中新旧类之间的相似性，并利用它来重建新嵌入空间中的原型。</p>
<ul>
<li>计算类别之间的相似性：
<img src="/EASE/image.png" alt="Alt text">
$P_{i,j}$:i指代第i个任务的数据，j指代第j个Adapter。在这里，n指代新任务，o指代旧任务
在此基础上继续进行归一化：$Sim_{i,j} = \frac{\text{exp}^{Sim_{i,j}}}{\sum_j\text{exp}^{Sim_{i,j}}}$</li>
</ul>
<p>在得到相似度矩阵后，我们进一步利用类别相似性矩阵在新的子空间中重建旧的类原型：
<img src="/EASE/image-1.png" alt="Alt text"></p>
<p>在此之后，我们就得到了最终的用于分类的原型分类器：
<img src="/EASE/image-2.png" alt="Alt text"></p>
<p>此外，为了进一步提高模型性能，作者对原型进行加权：对于经过同一Adapter的数据提高权重。
$$P^T_{b,b}\phi(x;\mathcal{A_b})+\alpha\sum_{i\ne b}P^T_{b,i}\phi(x;\mathcal{A_i})$$</p>
<h2>Experiments</h2>
<p><img src="/EASE/image-3.png" alt="Alt text"></p>
]]></content>
        <author>
            <name>FisherCat</name>
            <email>fishercat_@outlook.com</email>
            <uri>https://inspired-minds.vercel.app</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[Prototypical Network]]></title>
        <id>https://inspired-minds.vercel.app/blog/PrototypicalNetwork</id>
        <link href="https://inspired-minds.vercel.app/blog/PrototypicalNetwork"/>
        <updated>2024-05-27T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[NIPS2017 | It propose a new method of classification, by calculating the distance between instance and prototype.]]></summary>
        <content type="html"><![CDATA[<blockquote>
<p><a href="http://arxiv.org/abs/1703.05175">Paper</a></p>
</blockquote>
<h2>Core idea</h2>
<p>作者提出了一种新的分类方式，旨在利用类别的原型完成分类任务。通过计算测试数据与原型的“距离”，可以判断其属于哪一类。</p>
<p><img src="/Prototypical_Network/featured.png" alt="Alt text"></p>
<p>Left: Few-shot prototypes ck are computed as the mean of embedded support examples for each class. Right: Zero-shot prototypes ck are produced by embedding class meta-data vk. In either case, embedded query points are classified via a softmax over distances to class prototypes: $p\varphi(y = k|x) ∝ exp(−d(f_{\phi}(x), c_k))$.</p>
<h2>Prototypical Network</h2>
<h3>Prototypes</h3>
<p>每个原型$c_k \in R^M$是其所属类别特征的均值。$S_k$是总的数据量，$f_{\phi}$是特征提取函数。
![Alt text](/Prototypical Network/image.png)
模型预测方法：
![Alt text](/Prototypical Network/image-1.png)
d：距离计算函数(平方差距离)</p>
]]></content>
        <author>
            <name>FisherCat</name>
            <email>fishercat_@outlook.com</email>
            <uri>https://inspired-minds.vercel.app</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[Swin Transformer]]></title>
        <id>https://inspired-minds.vercel.app/blog/SwinTransformer</id>
        <link href="https://inspired-minds.vercel.app/blog/SwinTransformer"/>
        <updated>2024-03-06T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[ICCV 2021 best paper award.]]></summary>
        <content type="html"><![CDATA[<blockquote>
<p><a href="https://ieeexplore.ieee.org/document/9710580/">原文</a></p>
</blockquote>
<h2>Model Structure</h2>
<p><img src="/Swin_Transformer/image.png" alt="Alt text">
<img src="/Swin_Transformer/featured.png" alt="Alt text">
Swin Transformer在计算注意力时分成多个层级，每个层级将样本划分为不同大小的windows，然后在每一个windows中计算自注意力。<br>不同的层级windows的形状不同，所以可以使相邻层级之间的注意力计算产生联系。</p>
<h3>patch merging</h3>
<p><img src="/Swin_Transformer/image-6.png" alt="Alt text">
再对得到的张量在C 这个维度上进行卷积降维，变成 <strong>H/2 x W/2 x 2C</strong></p>
<h2>shifted window based self-attention</h2>
<p>典型的transformer结构计算全局的自注意力，而这样的计算复杂度与token的平方成正比，这使得它不适合许多需要大量tokens进行密集预测或表示高分辨率图像的视觉问题。</p>
<p>全局自注意力和基于窗口的注意力计算复杂度：
$$Ω(MSA) = 4hwC2 + 2(hw)2C,$$    </p>
<p>$$Ω(W-MSA) = 4hwC2 + 2M 2hwC$$</p>
<p><img src="/Swin_Transformer/image-1.png" alt="Alt text"></p>
<h2>Efficient batch computation for shifted configuration</h2>
<p>滑动窗口机制带来了一个问题，他会使windows数量增加。如在上图中，在layer l时windows数量为4，layer l+1时windows数量为9.有一些window的大小可能比M还要小     </p>
<p>我们提出了通过<em>cyclic-shifted</em> toward left-top direciton的更有效的批计算方法</p>
<p><img src="/Swin_Transformer/image-4.png" alt="Alt text"></p>
<p>经过这样的变换后，一个window中可能包括变换前的多个不相邻的子window，所以我们采取一种mask的方式来限制跨window的自注意力计算。通过cyclic-shifted，窗口的数量与常规窗口分区的数量相同，因此也很有效。</p>
<h2>Relative position bias</h2>
<p>$$ Attention(Q,K,V) = \text{SoftMax}(QK^T/\sqrt{d}+B)V $$
Q,K,V是查询,键和值矩阵，d是Q和K的维度。</p>
<p><img src="/Swin_Transformer/image-5.png" alt="Alt text"></p>
]]></content>
        <author>
            <name>FisherCat</name>
            <email>fishercat_@outlook.com</email>
            <uri>https://inspired-minds.vercel.app</uri>
        </author>
    </entry>
</feed>
